# Databricks Apps (Databricks Repos workflow):
# - Point the Repo at THIS directory (the repo root with app.yml).
# - After git pull locally or in CI, run: npm ci && npm run build  (committed dist/ is optional).
# - Set DB_HOST, DB_HTTP_PATH, DB_TOKEN (and FM/Genie vars as needed) in the app secrets — never commit tokens.
command:
  - bash
  - -c
  - |
      uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT

python:
  requirements_file: backend/requirements.txt

env:
  - name: DB_HOST
    value: ""
  - name: DB_HTTP_PATH
    value: ""
  - name: DB_TOKEN
    value: ""

resources:
  cpu: 1
  memory: 2Gi

port: 8000
